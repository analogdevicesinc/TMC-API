cmake_minimum_required (VERSION 3.8)
project ("TMC-API")
add_subdirectory ("doc")
add_subdirectory ("tmc")

# Look for an executable called myprogram
find_program(SPHINX_EXECUTABLE
             NAMES sphinx-build
             DOC "Path to myprogram executable")

# Check if the program was found
if(SPHINX_EXECUTABLE)
    message("Found myprogram: ${SPHINX_EXECUTABLE}")
else()
    message(FATAL_ERROR "Could not find SPHINX_EXECUTABLE")
endif()