(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XG_OFFS_TC_H(9:8)" => "XG_OFFS_TC_H98",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XG_OFFS_LP(5:0)" => "XG_OFFS_LP50",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XG_OFFS_TC_H(7:0)" => "XG_OFFS_TC_H70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YG_OFFS_TC_H(9:8)" => "YG_OFFS_TC_H98",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YG_OFFS_LP(5:0)" => "YG_OFFS_LP50",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YG_OFFS_TC_H(7:0)" => "YG_OFFS_TC_H70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZG_OFFS_TC_H(9:8)" => "ZG_OFFS_TC_H98",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZG_OFFS_LP(5:0)" => "ZG_OFFS_LP50",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZG_OFFS_TC_H(7:0)" => "ZG_OFFS_TC_H70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XA_ST_DATA(7:0)" => "XA_ST_DATA70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YA_ST_DATA(7:0)" => "YA_ST_DATA70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZA_ST_DATA(7:0)" => "ZA_ST_DATA70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XG_OFFS_USR(15:8)" => "XG_OFFS_USR158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XG_OFFS_USR(7:0)" => "XG_OFFS_USR70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YG_OFFS_USR(15:8)" => "YG_OFFS_USR158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YG_OFFS_USR(7:0)" => "YG_OFFS_USR70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZG_OFFS_USR(15:8)" => "ZG_OFFS_USR158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZG_OFFS_USR(7:0)" => "ZG_OFFS_USR70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "SMPLRT_DIV(7:0)" => "SMPLRT_DIV70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "DLPF_CFG(2:0)" => "DLPF_CFG20",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "EXT_SYNC_SET(2:0)" => "EXT_SYNC_SET20",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_XOUT(15:8)" => "ACCEL_XOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_XOUT(7:0)" => "ACCEL_XOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_YOUT(15:8)" => "ACCEL_YOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_YOUT(7:0)" => "ACCEL_YOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_ZOUT(15:8)" => "ACCEL_ZOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ACCEL_ZOUT(7:0)" => "ACCEL_ZOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "TEMP_OUT(15:8)" => "TEMP_OUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "TEMP_OUT(7:0)" => "TEMP_OUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_XOUT(15:8)" => "GYRO_XOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_XOUT(7:0)" => "GYRO_XOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_YOUT(15:8)" => "GYRO_YOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_YOUT(7:0)" => "GYRO_YOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_ZOUT(15:8)" => "GYRO_ZOUT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "GYRO_ZOUT(7:0)" => "GYRO_ZOUT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "FIFO_WM_TH(9:8)" => "FIFO_WM_TH98",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "FIFO_WM_TH(7:0)" => "FIFO_WM_TH70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "FIFO_COUNT(15:8)" => "FIFO_COUNT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "FIFO_COUNT(7:0)" => "FIFO_COUNT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XA_OFFSET(14:7)" => "XA_OFFSET147",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "XA_OFFSET(6:0)" => "XA_OFFSET60",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YA_OFFSET(14:7)" => "YA_OFFSET147",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "YA_OFFSET(6:0)" => "YA_OFFSET60",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZA_OFFSET(14:7)" => "ZA_OFFSET147",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/ICM_20602_hw_1.0.xml) Flat rename (register): "ZA_OFFSET(6:0)" => "ZA_OFFSET60",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "HARMCAL(4)" => "HARMCAL4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "HARMCAL(3:0)" => "HARMCAL30",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_MT(31:24)" => "OFFS_MT3124",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_MT(23:16)" => "OFFS_MT2316",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_MT(15:8)" => "OFFS_MT158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_MT(7:0)" => "OFFS_MT70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_ST(15:8)" => "OFFS_ST158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "OFFS_ST(7:0)" => "OFFS_ST70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MHM_hw_1.0.xml) Flat rename (register): "F_IO0(MCL)" => "F_IO0MCL",
Redefinition: IC_MHM_GAIN_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "RESABZ(7:0)" => "RESABZ70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "RESABZ(15:8)" => "RESABZ158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_ABZ(3:0)" => "OFF_ABZ30",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_ABZ(11:4)" => "OFF_ABZ114",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PROFILE_ID(7:0)" => "PROFILE_ID70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PROFILE_ID(15:8)" => "PROFILE_ID158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SERIAL(7:0)" => "SERIAL70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SERIAL(15:8)" => "SERIAL158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SERIAL(23:16)" => "SERIAL2316",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SERIAL(31:24)" => "SERIAL3124",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_ABZ(19:12)" => "OFF_ABZ1912",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_ABZ(27:20)" => "OFF_ABZ2720",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_ABZ(35:28)" => "OFF_ABZ3528",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_UVW(3:0)" => "OFF_UVW30",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "OFF_UVW(11:4)" => "OFF_UVW114",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PRES_POS(3:0)" => "PRES_POS30",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PRES_POS(11:4)" => "PRES_POS114",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PRES_POS(19:12)" => "PRES_POS1912",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PRES_POS(27:20)" => "PRES_POS2720",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "PRES_POS(35:28)" => "PRES_POS3528",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_BASE(3:0)" => "SPO_BASE30",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_0(3:0)" => "SPO_030",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_2(3:0)" => "SPO_230",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_1(3:0)" => "SPO_130",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_4(3:0)" => "SPO_430",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_3(3:0)" => "SPO_330",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_6(3:0)" => "SPO_630",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_5(3:0)" => "SPO_530",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_8(3:0)" => "SPO_830",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_7(3:0)" => "SPO_730",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_10(3:0)" => "SPO_1030",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_9(3:0)" => "SPO_930",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_12(3:0)" => "SPO_1230",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_11(3:0)" => "SPO_1130",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_14(3:0)" => "SPO_1430",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "SPO_13(3:0)" => "SPO_1330",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(7:0)" => "DEV_ID70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(15:8)" => "DEV_ID158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(23:16)" => "DEV_ID2316",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(31:24)" => "DEV_ID3124",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(39:32)" => "DEV_ID3932",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "DEV_ID(47:40)" => "DEV_ID4740",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "MFG_ID(7:0)" => "MFG_ID70",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "MFG_ID(15:8)" => "MFG_ID158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "CRC16(7:0)" => "CRC1670",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "CRC16(15:8)" => "CRC16158",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/IC_MU150_hw_1.0.xml) Flat rename (register): "CRC8(7:0)" => "CRC870",
Redefinition: TMC5041_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: //TMC5041_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM__STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RAMPMODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_A1_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_V1__MASK,
  same values, storing first occurance.
Redefinition: TMC5041_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_D1_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TZEROWAIT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XTARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IHOLD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IRUN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IHOLDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VCOOLTHRS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VDCMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STOP_L_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STOP_R_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POL_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POL_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SWAP_LR_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_L_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_L_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_R_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_R_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SG_STOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EN_SOFTSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_LATCH_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_LATCH_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_POS_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VELOCITY_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POSITION_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VZERO_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_T_ZEROWAIT_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SECOND_MOVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XLATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_MSCNT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CUR_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CUR_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEUP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEDN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEIMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SGT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SFILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DC_TIME_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DC_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SG_RESULT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FSACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CS_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STALLGUARD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STST_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC2130_hw_1.0.xml) Flat rename (register): "diag0_error (only with SD_MODE=1)" => "diag0_error_only_with_SD_MODE1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC2130_hw_1.0.xml) Flat rename (register): "diag0_otpw (only with SD_MODE=1)" => "diag0_otpw_only_with_SD_MODE1",
Redefinition: TMC2130_DIRECT_MODE_MASK,
  different values, commenting out
Redefinition: TMC2130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC2130_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_SG_MASK,
  different values, commenting out
Redefinition: TMC26x_STST_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_SG_MASK,
  different values, commenting out
Redefinition: //TMC26x_SG_MASK,
  different values, commenting out
Redefinition: TMC26x_STST_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_STST_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: //TMC26x_REGISTER_ADDRESS_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: //TMC26x_REGISTER_ADDRESS_BITS_MASK,
  same values, storing first occurance.
Redefinition: //TMC26x_REGISTER_ADDRESS_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: //TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: //TMC26x_REGISTER_ADDRESS_BITS_MASK,
  different values, commenting out
Redefinition: ////TMC26x_REGISTER_ADDRESS_BITS_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "start_en(0)" => "start_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "start_en(1)" => "start_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "start_en(2)" => "start_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "start_en(3)" => "start_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "start_en(4)" => "start_en4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "trigger_events(0)" => "trigger_events0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "trigger_events(1)" => "trigger_events1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "trigger_events(2)" => "trigger_events2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "trigger_events(3)" => "trigger_events3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "pipeline_en(0)" => "pipeline_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "pipeline_en(1)" => "pipeline_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "pipeline_en(2)" => "pipeline_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "pipeline_en(3)" => "pipeline_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(0)" => "XPIPE_REWRITE_REG0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(1)" => "XPIPE_REWRITE_REG1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(2)" => "XPIPE_REWRITE_REG2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(3)" => "XPIPE_REWRITE_REG3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(4)" => "XPIPE_REWRITE_REG4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(5)" => "XPIPE_REWRITE_REG5",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(6)" => "XPIPE_REWRITE_REG6",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "XPIPE_REWRITE_REG(7)" => "XPIPE_REWRITE_REG7",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4330_hw_1.1.xml) Flat rename (register): "MULTI_CYCLE_FAIL_F / SER_ENC_VAR_F" => "MULTI_CYCLE_FAIL_F__SER_ENC_VAR_F",
Redefinition: TMC4330_STOP_LEFT_IS_HOME_MASK,
  different values, commenting out
Redefinition: TMC4330_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ADDR_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_DATA_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4330_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_FIT_MASK,
  different values, commenting out
Redefinition: TMC4330_CL_FIT_MASK,
  different values, commenting out
Redefinition: //TMC4330_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_CL_BETA_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SIGN_AACT_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG1_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG2_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG5_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG5_VBREAK_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG6_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG6_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG8_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG8_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG9_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG9_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG10_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG10_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG10_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG11_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG11_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG11_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_OPERATION_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_RAMP_PROFILE_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG12_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SH_REG13_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SSI_IN_CLK_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_SSI_IN_WTIME_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VMEAN_FILTER_MASK,
  same values, storing first occurance.
Redefinition: TMC4330_ENC_VMEAN_INT_MASK,
  different values, commenting out
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "start_en(0)" => "start_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "start_en(1)" => "start_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "start_en(2)" => "start_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "start_en(3)" => "start_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "start_en(4)" => "start_en4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "trigger_events(0)" => "trigger_events0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "trigger_events(1)" => "trigger_events1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "trigger_events(2)" => "trigger_events2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "trigger_events(3)" => "trigger_events3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "pipeline_en(0)" => "pipeline_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "pipeline_en(1)" => "pipeline_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "pipeline_en(2)" => "pipeline_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "pipeline_en(3)" => "pipeline_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(0)" => "XPIPE_REWRITE_REG0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(1)" => "XPIPE_REWRITE_REG1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(2)" => "XPIPE_REWRITE_REG2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(3)" => "XPIPE_REWRITE_REG3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(4)" => "XPIPE_REWRITE_REG4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(5)" => "XPIPE_REWRITE_REG5",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(6)" => "XPIPE_REWRITE_REG6",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4331_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(7)" => "XPIPE_REWRITE_REG7",
Redefinition: TMC4331_STOP_LEFT_IS_HOME_MASK,
  different values, commenting out
Redefinition: TMC4331_MIXED_DECAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTO_DOUBLE_CHOPSYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SCALE_VALE_TRANSFER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SCK_LOW_BEFORE_CSN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_NEW_OUT_BIT_AT_RISE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SPI_OUT_LOW_TIME_MASK,
  different values, commenting out
Redefinition: TMC4331_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: TMC4331_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4331_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SG_MASK,
  different values, commenting out
Redefinition: TMC4331_OT_MASK,
  different values, commenting out
Redefinition: TMC4331_OTPW_MASK,
  different values, commenting out
Redefinition: TMC4331_S2GA_MASK,
  different values, commenting out
Redefinition: TMC4331_S2GB_MASK,
  different values, commenting out
Redefinition: TMC4331_OLA_MASK,
  different values, commenting out
Redefinition: TMC4331_OLB_MASK,
  different values, commenting out
Redefinition: TMC4331_STST_MASK,
  different values, commenting out
Redefinition: TMC4331_SG_MASK,
  different values, commenting out
Redefinition: //TMC4331_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_UV_SF_MASK,
  different values, commenting out
Redefinition: TMC4331_UV_SF_MASK,
  different values, commenting out
Redefinition: //TMC4331_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OTPW_MASK,
  different values, commenting out
Redefinition: //TMC4331_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OCA_MASK,
  different values, commenting out
Redefinition: TMC4331_OCB_MASK,
  different values, commenting out
Redefinition: TMC4331_OLA_MASK,
  different values, commenting out
Redefinition: //TMC4331_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OLB_MASK,
  different values, commenting out
Redefinition: //TMC4331_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OCHS_MASK,
  different values, commenting out
Redefinition: TMC4331_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SIGN_AACT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG1_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG2_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG5_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG5_VBREAK_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG6_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG6_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG8_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG8_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG9_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG9_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG10_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG10_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG10_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG11_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG11_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG11_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_OPERATION_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_RAMP_PROFILE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG12_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_SH_REG13_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLLING_STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_LOW_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLLING_REG_GSTAT_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLLING_REG_PWM_SCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_POLLING_REG_LOST_STEPS_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_COVER_HIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC4331_DAC_OFFSET_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "start_en(0)" => "start_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "start_en(1)" => "start_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "start_en(2)" => "start_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "start_en(3)" => "start_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "start_en(4)" => "start_en4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "trigger_events(0)" => "trigger_events0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "trigger_events(1)" => "trigger_events1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "trigger_events(2)" => "trigger_events2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "trigger_events(3)" => "trigger_events3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "pipeline_en(0)" => "pipeline_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "pipeline_en(1)" => "pipeline_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "pipeline_en(2)" => "pipeline_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "pipeline_en(3)" => "pipeline_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(0)" => "XPIPE_REWRITE_REG0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(1)" => "XPIPE_REWRITE_REG1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(2)" => "XPIPE_REWRITE_REG2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(3)" => "XPIPE_REWRITE_REG3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(4)" => "XPIPE_REWRITE_REG4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(5)" => "XPIPE_REWRITE_REG5",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(6)" => "XPIPE_REWRITE_REG6",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(7)" => "XPIPE_REWRITE_REG7",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361A_hw_1.0.xml) Flat rename (register): "MULTI_CYCLE_FAIL_F / SER_ENC_VAR_F" => "MULTI_CYCLE_FAIL_F__SER_ENC_VAR_F",
Redefinition: TMC4361_STOP_LEFT_IS_HOME_MASK,
  different values, commenting out
Redefinition: TMC4361_SPI_OUTPUT_FORMAT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MIXED_DECAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTO_DOUBLE_CHOPSYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SCALE_VALE_TRANSFER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTOREPEAT_COVER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_EXP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_ONLY_FOR_COVER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SCK_LOW_BEFORE_CSN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_NEW_OUT_BIT_AT_RISE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SPI_OUT_LOW_TIME_MASK,
  different values, commenting out
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRIVE_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_ON_ACC_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_ON_DEC_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_AFTER_START_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SEC_DRIVE_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREEWHEELING_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  different values, commenting out
Redefinition: TMC4361_BOOST_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  different values, commenting out
Redefinition: //TMC4361_DRV1_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ADDR_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_DATA_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  different values, commenting out
Redefinition: TMC4361_CL_FIT_MASK,
  different values, commenting out
Redefinition: //TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SG_MASK,
  different values, commenting out
Redefinition: TMC4361_OT_MASK,
  different values, commenting out
Redefinition: TMC4361_OTPW_MASK,
  different values, commenting out
Redefinition: TMC4361_S2GA_MASK,
  different values, commenting out
Redefinition: TMC4361_S2GB_MASK,
  different values, commenting out
Redefinition: TMC4361_OLA_MASK,
  different values, commenting out
Redefinition: TMC4361_OLB_MASK,
  different values, commenting out
Redefinition: TMC4361_STST_MASK,
  different values, commenting out
Redefinition: TMC4361_SG_MASK,
  different values, commenting out
Redefinition: //TMC4361_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_UV_SF_MASK,
  different values, commenting out
Redefinition: TMC4361_UV_SF_MASK,
  different values, commenting out
Redefinition: //TMC4361_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OTPW_MASK,
  different values, commenting out
Redefinition: //TMC4361_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OCA_MASK,
  different values, commenting out
Redefinition: TMC4361_OCB_MASK,
  different values, commenting out
Redefinition: TMC4361_OLA_MASK,
  different values, commenting out
Redefinition: //TMC4361_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLB_MASK,
  different values, commenting out
Redefinition: //TMC4361_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OCHS_MASK,
  different values, commenting out
Redefinition: TMC4361_UP_SCALE_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_SCALE_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SIGN_AACT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_X_LATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG1_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG2_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG5_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG5_VBREAK_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG6_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG6_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG8_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG8_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG9_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG9_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OPERATION_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_PROFILE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG12_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG13_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SSI_IN_CLK_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SSI_IN_WTIME_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VMEAN_FILTER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLLING_STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_LOW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLLING_REG_GSTAT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLLING_REG_PWM_SCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLLING_REG_LOST_STEPS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_HIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VMEAN_INT_MASK,
  different values, commenting out
Redefinition: TMC4361_DAC_OFFSET_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "start_en(0)" => "start_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "start_en(1)" => "start_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "start_en(2)" => "start_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "start_en(3)" => "start_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "start_en(4)" => "start_en4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "trigger_events(0)" => "trigger_events0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "trigger_events(1)" => "trigger_events1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "trigger_events(2)" => "trigger_events2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "trigger_events(3)" => "trigger_events3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "pipeline_en(0)" => "pipeline_en0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "pipeline_en(1)" => "pipeline_en1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "pipeline_en(2)" => "pipeline_en2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "pipeline_en(3)" => "pipeline_en3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(0)" => "XPIPE_REWRITE_REG0",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(1)" => "XPIPE_REWRITE_REG1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(2)" => "XPIPE_REWRITE_REG2",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(3)" => "XPIPE_REWRITE_REG3",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(4)" => "XPIPE_REWRITE_REG4",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(5)" => "XPIPE_REWRITE_REG5",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(6)" => "XPIPE_REWRITE_REG6",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "XPIPE_REWRITE_REG(7)" => "XPIPE_REWRITE_REG7",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4361_hw_1.0.xml) Flat rename (register): "MULTI_CYCLE_FAIL_F / SER_ENC_VAR_F" => "MULTI_CYCLE_FAIL_F__SER_ENC_VAR_F",
Redefinition: TMC4361_STOP_LEFT_IS_HOME_MASK,
  different values, commenting out
Redefinition: TMC4361_SPI_OUTPUT_FORMAT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MIXED_DECAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_AUTO_DOUBLE_CHOPSYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SCALE_VALE_TRANSFER_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_MULT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DISABLE_POLLING_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_MULT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POLL_BLOCK_MULT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SCK_LOW_BEFORE_CSN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_NEW_OUT_BIT_AT_RISE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SPI_OUT_LOW_TIME_MASK,
  different values, commenting out
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRIVE_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_ON_ACC_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_ON_DEC_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_BOOST_CURRENT_AFTER_START_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SEC_DRIVE_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREEWHEELING_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_CURRENT_SCALE_EN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  different values, commenting out
Redefinition: TMC4361_BOOST_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DRV1_SCALE_VAL_MASK,
  different values, commenting out
Redefinition: //TMC4361_DRV1_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_SCALE_VAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SINGLE_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MULTI_TURN_RES_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STATUS_BIT_CNT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ADDR_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_DATA_BITS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MSTEP_PER_FS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_TARGET_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_POS_COMP_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VEL_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_00_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_01_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_STATE_10_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MAX_PHASE_TRAP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FROZEN_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPL_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOPR_EVENT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOPL_ACTIVE_MASK,
  different values, commenting out
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_VSTOPL_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOME_ERROR_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_XLATCH_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FS_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_N_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_ENC_DATA_FAIL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SER_DATA_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SERIAL_ENC_FLAGS_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_DONE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VEL0_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_MAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_STOP_ON_STALL_MASK,
  different values, commenting out
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: //TMC4361_STOP_ON_STALL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_MOTOR_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RST_EV_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_CL_FIT_MASK,
  different values, commenting out
Redefinition: TMC4361_CL_FIT_MASK,
  different values, commenting out
Redefinition: //TMC4361_CL_FIT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SG_MASK,
  different values, commenting out
Redefinition: TMC4361_OT_MASK,
  different values, commenting out
Redefinition: TMC4361_OTPW_MASK,
  different values, commenting out
Redefinition: TMC4361_S2GA_MASK,
  different values, commenting out
Redefinition: TMC4361_S2GB_MASK,
  different values, commenting out
Redefinition: TMC4361_OLA_MASK,
  different values, commenting out
Redefinition: TMC4361_OLB_MASK,
  different values, commenting out
Redefinition: TMC4361_STST_MASK,
  different values, commenting out
Redefinition: TMC4361_SG_MASK,
  different values, commenting out
Redefinition: //TMC4361_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_UV_SF_MASK,
  different values, commenting out
Redefinition: TMC4361_UV_SF_MASK,
  different values, commenting out
Redefinition: //TMC4361_UV_SF_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OTPW_MASK,
  different values, commenting out
Redefinition: //TMC4361_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OCA_MASK,
  different values, commenting out
Redefinition: TMC4361_OCB_MASK,
  different values, commenting out
Redefinition: TMC4361_OLA_MASK,
  different values, commenting out
Redefinition: //TMC4361_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OLB_MASK,
  different values, commenting out
Redefinition: //TMC4361_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OCHS_MASK,
  different values, commenting out
Redefinition: TMC4361_UP_SCALE_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_HOLD_SCALE_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SIGN_AACT_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_FREQUENCY_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_X_LATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG1_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG2_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG3_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG4_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG5_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG5_VBREAK_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG6_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG6_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG8_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG8_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG9_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG9_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_BOW1_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG10_ASTART_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_BOW2_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG11_DFINAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_OPERATION_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_RAMP_PROFILE_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG12_BOW3_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SH_REG13_BOW4_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SSI_IN_CLK_DELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_SSI_IN_WTIME_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VMEAN_FILTER_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_LOW_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_COVER_HIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC4361_ENC_VMEAN_INT_MASK,
  different values, commenting out
Redefinition: TMC4361_DAC_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_APOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_BPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_NPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_USE_ABN_AS_N_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_CLN_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4670_DIRECTION_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "ADC_I0_RAW:" => "ADC_I0_RAW",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4671_hw_1.0.xml) Flat rename (register): "-" => "",
Redefinition: TMC4671_APOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_BPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_NPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_USE_ABN_AS_N_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_CLN_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_PID_TORQUE_ACTUAL_MASK,
  different values, commenting out
Redefinition: TMC4671_PID_TORQUE_TARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_PID_FLUX_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4671_PID_FLUX_TARGET_MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////////TMC4671__MASK,
  different values, commenting out
Redefinition: TMC4671__MASK,
  different values, commenting out
Redefinition: //TMC4671__MASK,
  different values, commenting out
Redefinition: ////TMC4671__MASK,
  different values, commenting out
Redefinition: //////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////////TMC4671__MASK,
  different values, commenting out
Redefinition: ////////////////TMC4671__MASK,
  different values, commenting out
Redefinition: //////////////////TMC4671__MASK,
  different values, commenting out
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "ADC_I0_RAW:" => "ADC_I0_RAW",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC4672_hw_1.0.xml) Flat rename (register): "-" => "",
Redefinition: TMC4672_APOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_BPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_NPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_USE_ABN_AS_N_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_CLN_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_DIRECTION_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_PID_TORQUE_ACTUAL_MASK,
  different values, commenting out
Redefinition: TMC4672_PID_TORQUE_TARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_PID_FLUX_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC4672_PID_FLUX_TARGET_MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC4672__MASK,
  different values, commenting out
Redefinition: //TMC4672__MASK,
  different values, commenting out
Redefinition: ////TMC4672__MASK,
  different values, commenting out
Redefinition: //////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////////TMC4672__MASK,
  different values, commenting out
Redefinition: ////////////////TMC4672__MASK,
  different values, commenting out
Redefinition: //////////////////TMC4672__MASK,
  different values, commenting out
Redefinition: TMC5031_DRV_ERR1_MASK,
  different values, commenting out
Redefinition: TMC5031_RAMPMODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_XACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_A1_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_V1__MASK,
  same values, storing first occurance.
Redefinition: TMC5031_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_D1_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TZEROWAIT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_XTARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_IHOLD_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_IRUN_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_IHOLDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VCOOLTHRS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STOP_L_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STOP_R_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_POL_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_POL_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SWAP_LR_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_LATCH_L_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_LATCH_L_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_LATCH_R_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_LATCH_R_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SG_STOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_EN_SOFTSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STATUS_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STATUS_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STATUS_LATCH_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STATUS_LATCH_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_EVENT_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_EVENT_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_EVENT_STOP_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_EVENT_POS_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VELOCITY_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_POSITION_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VZERO_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_T_ZEROWAIT_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SECOND_MOVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STATUS_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_XLATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS0_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS1_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS2_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS4_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS5_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS6_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS7_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS8_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS9_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS10_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS11_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS12_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS13_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS14_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS15_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS16_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS17_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS18_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS19_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS20_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS21_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS22_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS23_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS24_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS25_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS26_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS27_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS28_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS29_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS30_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS31_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS32_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS33_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS34_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS35_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS36_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS37_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS38_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS39_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS40_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS41_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS42_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS43_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS44_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS45_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS46_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS47_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS48_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS49_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS50_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS51_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS52_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS53_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS54_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS55_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS56_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS57_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS58_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS59_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS60_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS61_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS62_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS63_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS64_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS65_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS66_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS67_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS68_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS69_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS70_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS71_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS72_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS73_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS74_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS75_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS76_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS77_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS78_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS79_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS80_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS81_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS82_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS83_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS84_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS85_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS86_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS87_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS88_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS89_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS90_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS91_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS92_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS93_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS94_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS95_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS96_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS97_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS98_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS99_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS100_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS101_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS102_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS103_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS104_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS105_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS106_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS107_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS108_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS109_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS110_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS111_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS112_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS113_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS114_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS115_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS116_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS117_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS118_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS119_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS120_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS121_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS122_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS123_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS124_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS125_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS126_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS127_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS128_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS129_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS130_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS131_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS132_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS133_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS134_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS135_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS136_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS137_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS138_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS139_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS140_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS141_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS142_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS143_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS144_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS145_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS146_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS147_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS148_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS149_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS150_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS151_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS152_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS153_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS154_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS155_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS156_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS157_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS158_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS159_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS160_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS161_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS162_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS163_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS164_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS165_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS166_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS167_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS168_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS169_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS170_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS171_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS172_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS173_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS174_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS175_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS176_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS177_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS178_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS179_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS180_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS181_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS182_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS183_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS184_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS185_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS186_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS187_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS188_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS189_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS190_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS191_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS192_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS193_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS194_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS195_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS196_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS197_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS198_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS199_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS200_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS201_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS202_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS203_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS204_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS205_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS206_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS207_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS208_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS209_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS210_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS211_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS212_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS213_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS214_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS215_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS216_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS217_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS218_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS219_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS220_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS221_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS222_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS223_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS224_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS225_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS226_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS227_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS228_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS229_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS230_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS231_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS232_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS233_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS234_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS235_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS236_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS237_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS238_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS239_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS240_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS241_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS242_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS243_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS244_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS245_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS246_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS247_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS248_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS249_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS250_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS251_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS252_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS253_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS254_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFS255_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_W0_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_W1_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_W2_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_W3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_X1_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_X2_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_X3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_START_SIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_START_SIN90_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_MSCNT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CUR_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CUR_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SEMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SEUP_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SEMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SEDN_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SEIMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SGT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SFILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_SG_RESULT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_FSACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_CS_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STALLGUARD_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC5031_STST_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: //TMC5041_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_PWM__STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RAMPMODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_A1_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_V1__MASK,
  same values, storing first occurance.
Redefinition: TMC5041_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_D1_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TZEROWAIT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XTARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IHOLD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IRUN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_IHOLDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VCOOLTHRS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VDCMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STOP_L_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STOP_R_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POL_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POL_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SWAP_LR_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_L_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_L_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_R_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_LATCH_R_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SG_STOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EN_SOFTSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_LATCH_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_LATCH_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_STOP_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_EVENT_POS_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VELOCITY_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_POSITION_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VZERO_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_T_ZEROWAIT_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SECOND_MOVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STATUS_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_XLATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_MSCNT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CUR_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CUR_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEUP_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEDN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SEIMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SGT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SFILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DC_TIME_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_DC_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_SG_RESULT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_FSACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_CS_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STALLGUARD_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC5041_STST_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5062_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5062_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
Redefinition: TMC5062_DRV_ERR1_MASK,
  different values, commenting out
Redefinition: TMC5062_RAMPMODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_XACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_A1_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_V1__MASK,
  same values, storing first occurance.
Redefinition: TMC5062_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_D1_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TZEROWAIT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_XTARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_IHOLD_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_IRUN_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_IHOLDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VCOOLTHRS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STOP_L_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STOP_R_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POL_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POL_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SWAP_LR_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_LATCH_L_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_LATCH_L_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_LATCH_R_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_LATCH_R_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EN_LATCH_ENCODER_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SG_STOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EN_SOFTSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STATUS_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STATUS_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STATUS_LATCH_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STATUS_LATCH_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EVENT_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EVENT_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EVENT_STOP_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_EVENT_POS_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VELOCITY_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POSITION_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VZERO_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_T_ZEROWAIT_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SECOND_MOVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STATUS_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_XLATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POL_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POL_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POL_N_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_IGNORE_AB_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CLR_CONT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CLR_ONCE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_POS_EDGENEG_EDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CLR_ENC_X_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_LATCH_X_ACT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_ENC_SEL_DECIMAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_X_ENC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_INTEGER_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_FRACTIONAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_ENC_STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_ENC_LATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS0_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS1_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS2_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS4_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS5_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS6_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS7_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS8_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS9_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS10_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS11_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS12_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS13_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS14_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS15_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS16_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS17_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS18_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS19_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS20_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS21_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS22_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS23_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS24_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS25_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS26_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS27_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS28_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS29_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS30_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS31_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS32_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS33_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS34_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS35_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS36_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS37_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS38_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS39_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS40_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS41_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS42_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS43_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS44_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS45_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS46_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS47_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS48_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS49_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS50_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS51_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS52_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS53_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS54_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS55_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS56_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS57_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS58_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS59_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS60_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS61_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS62_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS63_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS64_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS65_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS66_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS67_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS68_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS69_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS70_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS71_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS72_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS73_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS74_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS75_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS76_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS77_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS78_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS79_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS80_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS81_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS82_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS83_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS84_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS85_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS86_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS87_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS88_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS89_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS90_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS91_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS92_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS93_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS94_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS95_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS96_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS97_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS98_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS99_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS100_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS101_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS102_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS103_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS104_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS105_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS106_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS107_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS108_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS109_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS110_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS111_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS112_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS113_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS114_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS115_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS116_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS117_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS118_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS119_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS120_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS121_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS122_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS123_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS124_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS125_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS126_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS127_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS128_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS129_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS130_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS131_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS132_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS133_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS134_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS135_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS136_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS137_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS138_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS139_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS140_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS141_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS142_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS143_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS144_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS145_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS146_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS147_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS148_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS149_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS150_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS151_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS152_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS153_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS154_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS155_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS156_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS157_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS158_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS159_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS160_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS161_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS162_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS163_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS164_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS165_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS166_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS167_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS168_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS169_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS170_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS171_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS172_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS173_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS174_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS175_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS176_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS177_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS178_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS179_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS180_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS181_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS182_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS183_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS184_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS185_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS186_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS187_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS188_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS189_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS190_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS191_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS192_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS193_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS194_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS195_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS196_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS197_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS198_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS199_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS200_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS201_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS202_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS203_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS204_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS205_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS206_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS207_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS208_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS209_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS210_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS211_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS212_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS213_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS214_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS215_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS216_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS217_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS218_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS219_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS220_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS221_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS222_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS223_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS224_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS225_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS226_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS227_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS228_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS229_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS230_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS231_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS232_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS233_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS234_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS235_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS236_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS237_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS238_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS239_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS240_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS241_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS242_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS243_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS244_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS245_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS246_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS247_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS248_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS249_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS250_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS251_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS252_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS253_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS254_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFS255_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_W0_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_W1_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_W2_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_W3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_X1_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_X2_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_X3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_START_SIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_START_SIN90_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_MSCNT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CUR_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CUR_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SEMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SEUP_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SEMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SEDN_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SEIMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SGT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SFILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_SG_RESULT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_FSACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_CS_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STALLGUARD_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC5062_STST_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5072_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5072_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
Redefinition: TMC5072_DRV_ERR1_MASK,
  different values, commenting out
Redefinition: TMC5072_SENDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AMPL_MASK,
  different values, commenting out
Redefinition: //TMC5072_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FREEWHEEL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_PWM_STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RAMPMODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_XACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSTART_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_A1_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_V1__MASK,
  same values, storing first occurance.
Redefinition: TMC5072_AMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_D1_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TZEROWAIT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_XTARGET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_IHOLD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_IRUN_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_IHOLDDELAY_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VCOOLTHRS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGH_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VDCMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STOP_L_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STOP_R_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POL_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POL_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SWAP_LR_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_L_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_L_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_R_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_R_INACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EN_LATCH_ENCODER_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SG_STOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EN_SOFTSTOP_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STATUS_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STATUS_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STATUS_LATCH_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STATUS_LATCH_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EVENT_STOP_L_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EVENT_STOP_R_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EVENT_STOP_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_EVENT_POS_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VELOCITY_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POSITION_REACHED_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VZERO_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_T_ZEROWAIT_ACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SECOND_MOVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STATUS_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_XLATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POL_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POL_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POL_N_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_IGNORE_AB_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CLR_CONT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CLR_ONCE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_POS_EDGENEG_EDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CLR_ENC_X_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_X_ACT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_ENC_SEL_DECIMAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_LATCH_NOW__MASK,
  same values, storing first occurance.
Redefinition: TMC5072_X_ENC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTEGER_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FRACTIONAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_ENC_STATUS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_ENC_LATCH_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MSCNT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CUR_A_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CUR_B_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_HSTRT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_HEND_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SEMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SEUP_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SEMAX_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SEDN_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SEIMIN_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SGT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SFILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DC_TIME_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_DC_SG_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_SG_RESULT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_FSACTIVE_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_CS_ACTUAL_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STALLGUARD_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OT_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OTPW_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_S2GA_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_S2GB_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OLA_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_OLB_MASK,
  same values, storing first occurance.
Redefinition: TMC5072_STST_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5130_hw_1.0.xml) Flat rename (register): "diag0_error (only with SD_MODE=1)" => "diag0_error_only_with_SD_MODE1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5130_hw_1.0.xml) Flat rename (register): "diag0_otpw (only with SD_MODE=1)" => "diag0_otpw_only_with_SD_MODE1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5130_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
Redefinition: TMC5130_I_SCALE_ANALOG_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_INTERNAL_RSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_EN_PWM_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_ENC_COMMUTATION_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_SHAFT_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DIAG0_INT_PUSHPULL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DIAG1_POSCOMP_PUSHPULL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_SMALL_HYSTERESIS_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_STOP_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TEST_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_RNDTF_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VSENSE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_SYNC_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_PWM_AMPL_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_PWM_GRAD_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_PWM_FREQ_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_PWM_AUTOSCALE_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_PWM_SYMMETRIC_MASK,
  same values, storing first occurance.
Redefinition: TMC5130_FREEWHEEL_MASK,
  same values, storing first occurance.
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5160_hw_1.0.xml) Flat rename (register): "diag0_error (only with SD_MODE=1)" => "diag0_error_only_with_SD_MODE1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5160_hw_1.0.xml) Flat rename (register): "diag0_otpw (only with SD_MODE=1)" => "diag0_otpw_only_with_SD_MODE1",
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC5160_hw_1.0.xml) Flat rename (register): "pos_edge/neg_edge" => "pos_edgeneg_edge",
Redefinition: TMC5160_RECALIBRATE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_FASTSTANDSTILL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_EN_PWM_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_MULTISTEP_FILT_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_SHAFT_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DIAG0_INT_PUSHPULL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DIAG1_POSCOMP_PUSHPULL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_SMALL_HYSTERESIS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_STOP_ENABLE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DIRECT_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TEST_MODE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160__MASK,
  different values, commenting out
Redefinition: TMC5160_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TFD_ALL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_OFFSET_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TFD_3_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISFDCC_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TPFD_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2VS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TOFF_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_CHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TBL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHFS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_VHIGHCHM_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TPFD_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_TPFD_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_MRES_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_INTPOL_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DEDGE_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2G_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2VS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160_DISS2VS_MASK,
  same values, storing first occurance.
Redefinition: TMC5160__MASK,
  different values, commenting out
Redefinition: //TMC5160__MASK,
  different values, commenting out
Redefinition: TMC5160__MASK,
  different values, commenting out
Redefinition: //TMC5160__MASK,
  different values, commenting out
Redefinition: ////TMC5160__MASK,
  different values, commenting out
Redefinition: TMC5160__MASK,
  different values, commenting out
Redefinition: //TMC5160__MASK,
  different values, commenting out
Redefinition: ////TMC5160__MASK,
  different values, commenting out
Redefinition: //////TMC5160__MASK,
  different values, commenting out
(C:/Users/tmctest/Documents/WSQt/TMCL-IDE/Plugins/Evalboard/TMC_XML/xml_descriptions/ic_xml/TMC6630_hw_1.0.xml) Flat rename (register): "HALL_EXT/START_VALUE" => "HALL_EXTSTART_VALUE",
Redefinition: TMC6630_SHORT_DELAY_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: ////////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: //////////TMC6630_UNUSED_MASK,
  different values, commenting out
Redefinition: TMC6630_UV_CP_MASK,
  different values, commenting out
Redefinition: TMC6630_HALL_ACTUAL_MASK,
  same values, storing first occurance.
done.